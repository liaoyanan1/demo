spring:
  application:
    name: b-service
  cloud:
    config:
      password: 123456
      username: usr
      discovery:
        enabled: true
        service-id: config-service
  profiles:
    active: dev

server:
  port: 9090
  servlet:
    context-path: /B
    session:
      cookie:
        name: BSESSION
management:
  endpoints:
    web:
      exposure:
        include: "*"