spring:
  application:
    name: gateway-service
  cloud:
    config:
     password: 123456
     username: usr
     discovery:
       enabled: true
       service-id: config-service
  profiles:
    active: dev

server:
  port: 9999

