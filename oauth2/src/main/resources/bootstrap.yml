spring:
  application:
    name: oauth2
  cloud:
    config:
      discovery:
        service-id: config-service
        enabled: true
      username: usr
      password: 123456
  profiles:
    active: dev
