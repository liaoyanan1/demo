spring:
  cloud:
    config:
      password: 123456
      username: usr
      discovery:
        enabled: true
        service-id: config-service
  application:
    name: a-service
  profiles:
    active: dev
server:
  port: 8080
  servlet:
    context-path: /A
    session:
      cookie:
        name: ASESSION
management:
  endpoints:
    web:
      exposure:
        include: "*"