spring:
  application:
    name: discovery-service
  cloud:
    config:
      password: 123456
      username: usr
      uri: http://localhost:8888
      fail-fast: true

server:
  port: 8761

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
